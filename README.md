# AI Murder Mystery Detective

> A noir detective game where every case is generated by AI, every suspect speaks in their own voice, and no two playthroughs are ever the same.

**Live demo:** [hackathon-2026-05-ahmed-mujtaba-eta.vercel.app](https://hackathon-2026-05-ahmed-mujtaba-eta.vercel.app)

---

## What it is

You step into the role of a 1947 detective. The AI generates a unique murder case — victim, suspects, witnesses, weapon, motive, and the truth — and gives you a fixed amount of time to find the killer. You read the case file, interrogate suspects in natural language, request a single hint when you're stuck, and finally make your accusation. The AI narrates every clue and reveal in suspect-specific voices. Every case is a one-of-a-kind story.

---

## Why it stands out

| Feature | Detail |
|---|---|
| **Fully AI-generated cases** | Groq's `llama-3.3-70b-versatile` generates the victim, ≥3 suspects with private truths, witnesses, alibis, motives, red herrings, and a full reveal narrative — every game, no templates |
| **Natural-language interrogation** | The AI takes the suspect's persona, secrets, and conversation history into account and replies in-character. Push the right way and the suspect cracks |
| **Voice on both sides** | TTS reads the case file and suspect responses in **gender-aware voices** with deterministic per-character pitch. Voice INPUT lets you literally speak your questions to the suspect — no API key, no server cost (Web Speech API) |
| **Coin economy** | Cases cost coins up front (50 / 100 / 200), reward coins on a win, deduct nothing extra on a loss. All transactions are wrapped in DB transactions in `CoinService` |
| **Live timer** | 25 / 35 / 55 minutes per difficulty, runs client-side, auto-submits as a loss on expiry |
| **Hint system** | One hint per case, first-person ("Something about Maria's alibi doesn't sit right…"), enforced by `HintService.requestHint()` throwing `HintAlreadyUsedError` on second call |
| **Verdict reveal** | Win or lose, the actual murderer is revealed by name on a stamped *Classified* dossier card, followed by an AI-written cinematic narrative |
| **First-time onboarding** | A 5-step skippable welcome tour mounts once on the dashboard, persisted via `localStorage` (`mm_onboarded_v1`) |
| **Stats tracking** | Wins, losses, win-rate per difficulty stored in `localStorage` and rendered as animated counters and a perf bar chart |
| **Production-grade observability** | PostHog (frontend, 19 typed events) + Sentry (backend, 5xx-only filter with user/request context) |
| **Full E2E coverage** | 40 Playwright tests across 6 suites running against the live deployment |

---

## Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│  Vercel — Next.js 16 App Router (React 19, framer-motion)        │
│  ─ Cinzel + Cormorant Garamond + Outfit + JetBrains Mono         │
│  ─ Web Audio API (procedural SFX + thunder synced to canvas)     │
│  ─ Web Speech API (TTS + STT, fully client-side)                 │
│  ─ PostHog client (19 typed events, route-aware page_view)       │
└────────────────────────┬─────────────────────────────────────────┘
                         │   REST + JWT (Authorization: Bearer)
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  Railway — NestJS 10 (Node 22, TypeScript 5.4)                   │
│  ┌────────────┬────────────┬────────────┬────────────┐           │
│  │ AuthModule │ CaseModule │ HintModule │VerdictModule│  + AI    │
│  │  Google    │ generates  │ one hint   │ reveal +    │  Module  │
│  │  OAuth +   │ a case via │ per case,  │ win/lose    │ (Groq)   │
│  │  JWT       │ Groq SDK   │ persisted  │ + coin      │          │
│  └────────────┴────────────┴────────────┴────────────┘           │
│  ─ Sequelize (Postgres on Neon) — User, GameSession, Turn        │
│  ─ Sentry — global filter, 5xx + user/request scope              │
│  ─ /health endpoint for Railway health checks                    │
└──────────────────────────────────────────────────────────────────┘
```

**Five most important files:**
- `backend/src/case/case.service.ts` — case lifecycle (start, get, sanitize)
- `backend/src/verdict/verdict.service.ts` — submit verdict, reveal, coin transactions
- `backend/src/ai/ai.service.ts` — Groq SDK wrapper + 4 prompt templates
- `backend/src/coin/coin.service.ts` — `deduct()` / `award()`, transactional
- `frontend/src/app/game/[sessionId]/page.tsx` — game loop UI

---

## Tech stack

| Layer | Tech |
|---|---|
| Frontend | Next.js 16 App Router, React 19, TypeScript 5, Tailwind v3, framer-motion |
| Fonts | Cinzel (display), Cormorant Garamond (serif), Outfit (sans), JetBrains Mono (mono) |
| Backend | NestJS 10, Node 22, Sequelize ORM |
| DB | Neon Postgres (serverless) |
| AI | Groq SDK — `llama-3.3-70b-versatile` |
| Auth | Google OAuth → JWT (passport-google-oauth20 + passport-jwt) |
| Audio | Web Audio API (procedural SFX), Web Speech API (TTS + STT) |
| Analytics | PostHog (custom client, autocapture disabled) |
| Errors | Sentry (`@sentry/node`, custom NestJS exception filter) |
| Tests | Playwright 1.59 — 40 tests, 6 suites, run against live URL |
| CI/CD | GitHub → Vercel (frontend), GitHub → Railway (backend) |

---

## Quick start

```bash
# Backend
cd backend
npm install
cp .env.example .env       # fill in Google OAuth, GROQ_API_KEY, DB creds
npm run db:migrate
npm run dev                # runs on :3001

# Frontend (separate terminal)
cd frontend
npm install
cp .env.example .env.local  # set NEXT_PUBLIC_API_URL=http://localhost:3001
npm run dev                 # runs on :3000

# Both at once (from repo root)
npm run dev
```

---

## E2E tests

```bash
cd e2e
npm install
npx playwright install chromium

# Public suites (no auth required)
npm run test:public
# → 01-landing, 02-auth-callback, 03-api-health, 05-responsive-ui

# All suites (needs JWT for authenticated flows)
TEST_JWT=<your-jwt> npm test
# → 40 tests across 6 suites
```

---

## Key invariants (encoded in code, not just docs)

- **Suspect secrets never leak.** `case.service.ts → sanitizeCase()` strips `private_truth`, `alibi_is_true`, `will_crack_if`, `secrets`, and `murderer_id` before sending the case to the frontend. The frontend literally cannot know the answer until verdict submission.
- **Coins can never go negative.** `CoinService.deduct()` throws `InsufficientCoinsError` inside the same DB transaction that creates the game session — atomic.
- **One hint per case.** `HintService.requestHint()` throws `HintAlreadyUsedError` on second call, even with race conditions.
- **Frontend never calls Groq directly.** All AI calls live inside `backend/src/ai/`. The frontend only knows about REST endpoints.
- **Background music goes silent the moment a case starts.** The new-game button stops both ambient and music tracks before navigation, so the cinematic generation overlay and the entire game session are silent. Music resumes when the player returns to the dashboard.

---

## Production observability

**PostHog** captures 19 typed events covering the full funnel:
`sign_in_clicked → auth_success → case_started → investigation_begun → suspect_selected → question_sent → answer_received → hint_requested → hint_received → accusation_panel_opened → verdict_submitted → verdict_result → play_again_clicked`

User identity is set via `phIdentify(user.id, { email, coin_balance })` on every session load and reset on logout. Dev builds auto opt-out (`ph.opt_out_capturing()`) so test plays don't pollute production data.

**Sentry** uses a global NestJS exception filter (`SentryExceptionFilter`) that:
- Captures only 5xx errors (skips `InsufficientCoinsError`, `HintAlreadyUsedError`, validation 400s, etc.)
- Attaches authenticated user (`id`, `email`), request method/URL/body, and `http.status` tag via `Sentry.withScope()`
- Disabled when `SENTRY_DSN` is unset, so local dev generates zero noise

---

## Project layout

```
hackathon-2026-05-AhmedMujtaba/
├── backend/                  # NestJS 10 API
│   ├── src/
│   │   ├── instrument.ts     # Sentry init — must import first
│   │   ├── main.ts           # bootstrap + CORS + /health
│   │   ├── ai/               # Groq + 4 prompt templates
│   │   ├── auth/             # Google OAuth + JWT
│   │   ├── case/ hint/ interrogate/ verdict/
│   │   ├── coin/             # transactional balance ops
│   │   ├── common/
│   │   │   ├── errors/       # typed game exceptions
│   │   │   └── filters/      # SentryExceptionFilter
│   │   └── db/               # Sequelize models + migrations
│   └── railway.json          # explicit build/start for Railway
├── frontend/                 # Next.js 16 SPA
│   └── src/
│       ├── app/              # App Router pages
│       ├── components/
│       │   ├── game/         # CaseFileBook, InterrogationChat, etc.
│       │   ├── onboarding/   # First-time tour
│       │   └── providers/    # PostHogProvider
│       └── lib/
│           ├── api.ts        # typed fetch wrapper
│           ├── audio.ts      # Web Audio engine
│           ├── tts.ts        # Web Speech TTS
│           └── posthog.ts    # event catalogue + init
├── e2e/                      # Playwright tests against live URL
├── phases/                   # Phase docs (1–9)
└── *.md                      # SPEC, DEMO_SCRIPT, COST_LOG, etc.
```

---

## Hackathon submission

| Item | Status |
|---|---|
| Full E2E playable (auth → case → interrogate → verdict) | ✅ |
| Frontend deployed (Vercel) | ✅ |
| Backend deployed (Railway) | ✅ |
| Database (Neon Postgres) migrated | ✅ |
| AI integration (Groq) | ✅ |
| Voice TTS + STT | ✅ |
| Stats / progression | ✅ |
| Onboarding tour | ✅ |
| Analytics (PostHog) | ✅ |
| Error monitoring (Sentry) | ✅ |
| 40 E2E tests passing against live | ✅ |

See [SPEC.md](./SPEC.md) for the full feature spec and [DEMO_SCRIPT.md](./DEMO_SCRIPT.md) for the 5-minute demo run-of-show.
